openapi: "3.1.0"
paths:
  /api/skus:
    post:
      summary: "Skus create"
      responses:
        "201":
          description: "Created"
    get:
      summary: "Skus list"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}/reject:
    post:
      summary: "Recommendations reject"
      responses:
        "202":
          description: "Accepted"
  /api/skus/{id}:
    delete:
      summary: "Skus delete"
      responses:
        "200":
          description: "OK"
    patch:
      summary: "Skus update"
      responses:
        "200":
          description: "OK"
    get:
      summary: "Skus get"
      responses:
        "200":
          description: "OK"
  /api/inventory/{id}:
    put:
      summary: "Inventory update"
      responses:
        "200":
          description: "OK"
  /tenants/me:
    get:
      summary: "Tenant me"
      responses:
        "200":
          description: "OK"
  /api/users/{id}:
    patch:
      summary: "Users update"
      responses:
        "200":
          description: "OK"
  /api/notifications:
    get:
      summary: "Notifications list"
      responses:
        "200":
          description: "OK"
  /api/warehouses/{id}:
    delete:
      summary: "Warehouses delete"
      responses:
        "200":
          description: "OK"
    patch:
      summary: "Warehouses update"
      responses:
        "200":
          description: "OK"
    get:
      summary: "Warehouses get"
      responses:
        "200":
          description: "OK"
  /api/tenants/register:
    post:
      summary: "Tenant register"
      responses:
        "200":
          description: "OK"
  /api/simulations/{id}/cancel:
    post:
      summary: "Simulations cancel"
      responses:
        "202":
          description: "Accepted"
  /api/demand-history:
    get:
      summary: "Demand history list"
      responses:
        "200":
          description: "OK"
  /api/policies:
    post:
      summary: "Policies create"
      responses:
        "201":
          description: "Created"
    get:
      summary: "Policies list"
      responses:
        "200":
          description: "OK"
  /api/inventory:
    get:
      summary: "Inventory list"
      responses:
        "200":
          description: "OK"
  /api/integrations/status:
    get:
      summary: "Integrations status"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}/export:
    post:
      summary: "Recommendations export"
      responses:
        "202":
          description: "Accepted"
  /api/notifications/{id}/read:
    patch:
      summary: "Notifications read"
      responses:
        "200":
          description: "OK"
  /api/lanes:
    post:
      summary: "Lanes create"
      responses:
        "201":
          description: "Created"
    get:
      summary: "Lanes list"
      responses:
        "200":
          description: "OK"
  /api/settings/tenant:
    patch:
      summary: "Tenant settings update"
      responses:
        "200":
          description: "OK"
    get:
      summary: "Tenant settings read"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}/approve:
    post:
      summary: "Recommendations approve"
      responses:
        "202":
          description: "Accepted"
  /health:
    get:
      summary: "Health"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}/expire:
    post:
      summary: "Recommendations expire"
      responses:
        "202":
          description: "Accepted"
  /health/db:
    get:
      summary: "Health db"
      responses:
        "200":
          description: "OK"
  /metrics:
    get:
      summary: "Metrics"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}:
    get:
      summary: "Recommendations get"
      responses:
        "200":
          description: "OK"
  /api/users:
    post:
      summary: "Users create"
      responses:
        "201":
          description: "Created"
    get:
      summary: "Users list"
      responses:
        "200":
          description: "OK"
  /api/recommendations/{id}/export.csv:
    get:
      summary: "Recommendations export csv"
      responses:
        "200":
          description: "OK"
  /api/warehouses:
    post:
      summary: "Warehouses create"
      responses:
        "201":
          description: "Created"
    get:
      summary: "Warehouses list"
      responses:
        "200":
          description: "OK"
  /api/imports:
    post:
      summary: "Imports create"
      responses:
        "202":
          description: "Accepted"
  /api/recommendations:
    get:
      summary: "Recommendations list"
      responses:
        "200":
          description: "OK"
  /api/settings/api-key/rotate:
    post:
      summary: "Api key rotate"
      responses:
        "200":
          description: "OK"
  /api/simulations/{id}:
    get:
      summary: "Simulations get"
      responses:
        "200":
          description: "OK"
  /api/imports/{id}:
    get:
      summary: "Imports result"
      responses:
        "200":
          description: "OK"
  /api/integrations/test:
    post:
      summary: "Integrations test"
      responses:
        "202":
          description: "Accepted"
  /health/ready:
    get:
      summary: "Health ready"
      responses:
        "200":
          description: "OK"
  /api/simulations:
    post:
      summary: "Simulations create"
      responses:
        "202":
          description: "Accepted"
    get:
      summary: "Simulations list"
      responses:
        "200":
          description: "OK"
info:
  title: "Inventory Allocation Simulator API"
  version: "0.1.0"
